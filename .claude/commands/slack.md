Use the `slack-ordering-agent` sub-agent to handle this Slack notification task:

$ARGUMENTS

The agent covers ordering doc notifications, Block Kit message design, webhook configuration, and per-venue staff routing in GoogleDocsPrepSystem.gs.
