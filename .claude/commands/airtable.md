Use the `airtable-schema-agent` sub-agent to handle this Airtable task:

$ARGUMENTS

The agent covers base structure, table schemas, linked record fields, formula fields, views, and Airtable REST API query patterns for both venues.
