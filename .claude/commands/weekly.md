Use the `weekly-cycle-agent` sub-agent to handle this weekly cycle task:

$ARGUMENTS

The agent covers ClearWeeklyCount, FinaliseCount, GeneratePrepRun, and time-based polling scripts for both venues. It will treat all changes as high-risk and require testing on a copy before production.
