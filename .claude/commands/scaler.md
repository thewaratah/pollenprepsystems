Use the `recipe-scaler-agent` sub-agent to handle this Recipe Scaler task:

$ARGUMENTS

The agent covers RecipeScaler.gs, RecipeScalerUI.html, and GAS web app deployment for the scaler. It knows the Waratah linked-record vs Sakura plain-text recipe name difference.
