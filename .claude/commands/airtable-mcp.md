Use the `airtable-mcp-agent` sub-agent to handle this live Airtable data task:

$ARGUMENTS

The agent uses the MCP2 server for live record queries — checking field values, filtering records, verifying counts, and inspecting linked record relationships across both venue bases (appfcy14ZikhKZnRS = Waratah, appNsFRhuU47e9qlR = Sakura). For schema design or structural changes, use /airtable instead.
