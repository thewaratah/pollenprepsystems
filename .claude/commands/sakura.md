Use the `sakura-prep-agent` sub-agent to handle this Sakura House PREP system task:

$ARGUMENTS

The agent will read `Sakura House/CLAUDE.md` first, then implement or investigate as needed. It specialises in the Sakura GAS + Airtable pipeline, `Recipe Name` as a direct text field, and ensuring no Waratah-specific patterns bleed into Sakura code.
